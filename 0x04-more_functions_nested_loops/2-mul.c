#include <stdio.h>

/**
 * mul - a function that multiplies two numbers
 *
 * @a: first number
 * @b: second number
 * Return: returns the result on multiplication
 *
 */

int mul(int a, int b)
{
	return (a * b);
}
