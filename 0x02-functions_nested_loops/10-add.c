#include <stdio.h>

/**
 * add - a function that adds two integers
 *
 * @a: the first integers to add
 * @b: the second integer to add
 * Return: return the result of the addition
 */

int add(int a, int b)
{
	return (a + b);
}
