#include<stdio.h>

/**
 * main - prints a message using printf
 *
 * Description: This function will use the printf function to print a message
 * Return: returns 0 if everything went good!
 */
int main(void)
{
printf("%s", "with proper grammar, but the outcome is a piece of art,\n");

return (0);
}
