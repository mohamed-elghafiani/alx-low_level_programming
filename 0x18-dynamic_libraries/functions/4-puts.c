#include<stdio.h>

/**
 * main - prints a message using puts
 *
 * Description: This function will use the puts function to print a message
 * Return: returns 0 if everything went good!
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");

return (0);
}
